export enum AgentFocusEnum {
  FRONTEND = "frontend",
  BACKEND = "backend",
  AGNOSTIC = "agnostic",
}

export enum AgentSpecialtyEnum {
  NONE = "none",
  LANDING_PAGES = "landing_pages",
  FORMS = "forms",
  REFACTOR = "refactor",
}
