version: "3.8"

services:
  ui:
    build: ./ui
    ports:
      - "8080:80"
    restart: always
  api:
    build: ./api
    ports:
      - "4000:4000"
    env_file:
      - ./api/.env
    restart: always
